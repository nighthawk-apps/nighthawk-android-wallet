package com.nighthawkapps.lib.android.ui.screenshot

import org.junit.Test

/** Screenshot baselines were removed with the DarkFi-only refactor; replace once Compose previews stabilize again. */
class ScreenshotTest {
    @Test
    fun placeholder() {
        // Intentionally empty — prevents an unused Gradle module from failing compilation.
    }
}
